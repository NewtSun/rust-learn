use crate::basic::bind::bind;

mod basic;

// use std::cmp::Ordering;

// use rand::Rng;
// use crate::basic::bind::bind;

fn main() {
    // let number = rand::thread_rng().gen_range(1..=10);
    //
    // println!("rand {}", number);
    //
    // match number.cmp(&10) {
    //     Ordering::Less => println!("too small"),
    //     Ordering::Greater => println!("too small"),
    //     Ordering::Equal => println!("ok"),
    // }
    //
    // let a = "aaaa";
    // let b: u32 = match a.trim().parse() {
    //     Ok(num) => num,
    //     Err(_) => 0,
    // };

    bind()
}
